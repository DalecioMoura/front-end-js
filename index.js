let rotaIndex = window.location;
sessionStorage.rotaIndex = rotaIndex;

if(sessionStorage.rotaIndex = window.location)
    console.log('rota: '+ rotaIndex)
else{
    console.log(sessionStorage.rotaIndex);
    console.log(window.location);
}